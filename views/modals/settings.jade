#settings.modal
	header.bar.bar-nav
		a.icon.icon-close.pull-right(href="#settings")
		h1.title Settings

	.content
		ul.table-view.settings
			li.table-view-cell Display duration of each schedule block
				.toggle
					.toggle-handle
		if admin
			a.btn.btn-primary.btn-block(href="/admin") Admin Panel
			br
		if loggedIn
			button#signout.btn.btn-negative.btn-block Sign Out
			br